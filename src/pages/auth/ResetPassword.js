import React, { Component } from 'react'
import { Text, View } from 'react-native'

class ResetPasswordScene extends Component {
  render() {
    return (
      <View>
        <Text> Reset Password Scene </Text>
      </View>
    )
  }
}

export default ResetPasswordScene;